import { ScrollView, Text } from "react-native";
export default function Booking(){
    return (
        <ScrollView>
            <Text style={{marginTop: 300, textAlign: "center"}}>Booking</Text>
        </ScrollView>
    )
}