import { ScrollView, Text } from "react-native";

export default function Shopping(){
    return (
        <ScrollView>
            <Text style={{marginTop: 300, textAlign: "center"}}>Shopping</Text>
        </ScrollView>
    )
}