import {ScrollView, Text} from "react-native";

export default function Cart(){
    return (
        <ScrollView>
            <Text style={{marginTop: 300, textAlign: "center"}}>Cart</Text>
        </ScrollView>
    )
}